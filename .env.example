# Docker Compose Environment Variables
# Copy to .env and customize as needed

# HuggingFace cache location (shared between vLLM and voice-agent)
HF_CACHE=~/.cache/huggingface

# HuggingFace token (optional, for gated models)
# HUGGING_FACE_HUB_TOKEN=your_token_here

# Voice agent port
VOICE_AGENT_PORT=3000

# LLM Base URL (default: uses the vllm docker service)
# Uncomment to use an external LLM server instead of the docker vllm service
# LLM_BASE_URL=http://your-llm-server:8000/v1

# SearXNG URL for web search MCP server (optional)
# SEARXNG_URL=https://your-searxng-instance.example.com
